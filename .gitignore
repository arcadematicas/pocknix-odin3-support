.git/
*.dtb

# local backups (firmware/abl copies) that must never be committed
backups/

# bootanimation original de la ROM (105 MB) — excede el limite de 100 MB de
# GitHub. Se conserva localmente; ver config/splash/README.md para extraerlo.
config/bootanimation-odin3.zip
